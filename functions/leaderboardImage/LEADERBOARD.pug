html
  head
    title Leaderboard
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
    style.
      table {
        border-collapse: collapse;
        width: 388px;
        background-color: #F5F5F5;
        border: 0.5px solid #3a3a3a;
        font-family: 'Montserrat', sans-serif;
        font-size: 14px;
      }
      .leaderboard-header {
        background-color: black;
        width: 386px;
        color: #CCF900;
        padding: 1px;
        text-align: center;
        font-family: 'Montserrat', sans-serif;
      }
      th, td {
        padding-top: 8px;
        padding-right: 4px;
        padding-bottom: 8px;
        padding-left: 4px;
        text-align: center;
      }
      th {
        background-color: #ccf900;
        color: black;
        box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
        border-radius: 5px;
      }
      /*tr:nth-child(even) {
        background-color: #F5F5F5;
      }*/
      /*.top-three {
        background-color: rgba(255, 255, 255, 0.9);
        border: 0.2px solid #3a3a3a;
        box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
        border-radius: 25px;
      }*/
      .school-column {
        font-size: 11px;
      }
      .player-column {
        font-weight: bold;
      }
  body
    .leaderboard-header
        h3 LEADERBOARD
    table
      thead
        tr
          th Rank
          th Player
          th School
          th Grade
      tbody
        each player, index in players.slice(0, leaderboardLimit)
            tr
              td= player.Rank
              td(class='player-column')= player.Player
              td= player.School
              td= player.Class

